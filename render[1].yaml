services:
  - type: web
    name: production-ii-kpp-survey
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python survey_app.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
